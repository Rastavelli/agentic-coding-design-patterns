# Git hooks managed with lefthook (https://github.com/evilmartians/lefthook).
# Installed automatically on `pnpm install`; see Makefile.
commit-msg:
  commands:
    commitlint:
      run: pnpm exec commitlint --edit {1}
